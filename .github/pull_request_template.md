## Linked issue

Closes #

## What changed

## Checklist

- [ ] Acceptance criteria on the linked issue are met
- [ ] Build passes locally (`npm run build` / `cargo test`)
- [ ] No data fetching outside a DataProvider (web PRs)
- [ ] Docs updated if a public interface changed
- [ ] Commits signed off (DCO, `git commit -s`)
